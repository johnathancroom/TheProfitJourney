<%= form_for @technician, :url => path, :html => { :class => "styled_form" } do |f| %>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <%= f.submit "Submit", :class => "button" %>
<% end %>